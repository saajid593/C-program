#include <stdio.h>
int main() {
    int row,col;
    printf("Enter the number of rows: ");
    scanf("%d",&row);
    for (int i =0;i<row;i++)
    {
        for (int i =0;i<row; i++)
        {
            /* code */
        }
        
    }
    
}